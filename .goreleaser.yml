version: 2

project_name: gh-autoprofile

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/gh-autoprofile
    binary: gh-autoprofile
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mdiloreto/gh-autoprofile/internal/cmd.Version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: mdiloreto
    name: gh-autoprofile
  draft: false
  prerelease: auto
